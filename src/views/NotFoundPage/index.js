import React from 'react';
import Layout from "../../components/Layout";

const NotFound = () => {
    return (
        <Layout>
            Not Found Page
        </Layout>
    );
};

export default NotFound;